import SwiftUI
import shared

struct ContentView: View {
    var body: some View {
        MasterScreen()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        MasterScreen()
    }
}
